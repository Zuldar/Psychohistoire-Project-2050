name: Mise à jour Mensuelle Psychohistoire

on:
  schedule:
    - cron: '0 8 1 * *' # Tous les 1er du mois à 8h
  workflow_dispatch:      # Permet de lancer manuellement

permissions:
  contents: write

jobs:
  run-model:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Récupération du dépôt
      uses: actions/checkout@v3

    - name: 🐍 Installation de Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: 🧠 Exécution du modèle Psychohistorique
      run: |
        python src/main_analyzer.py

    - name: 💾 Commit et Push des résultats
      run: |
        git config --global user.name 'Psychohistory Bot'
        git config --global user.email 'bot@psychohistory.com'
        # ICI : J'ai ajouté data/projection_2036.json
        git add data/current_state.json data/projection_2036.json reports/
        # On ne commit que si ça a changé
        git diff --quiet && git diff --staged --quiet || (git commit -m "🔮 Mise à jour : Projection Seldon 2036" && git push origin HEAD:
        main)
